import { InterceptorsInterceptor } from './graphqlCache.interceptor';

describe('InterceptorsInterceptor', () => {
  it('should be defined', () => {
    expect(new InterceptorsInterceptor()).toBeDefined();
  });
});
